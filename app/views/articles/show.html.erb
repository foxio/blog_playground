<h1> <%= @article.title %> </h1>

<p>
  Tags:
  <% @article.tags.each do |tag| %>
    <%= tag.name %><%= "," unless tag == @article.tags.last %>
  <% end %>
</p>

<p>
  <% minutes = (@article.read_time.to_f/60).round %>
  <% time = "about #{pluralize(minutes, "minute")}" %>
  <% time = "Less than a minute" if minutes == 0 %>
  Read time- <%= time %> 
</p>

<p>
  <%= @article.body %>
</p>

<%= link_to 'Edit', edit_article_path(@article) %>
